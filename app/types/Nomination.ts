export interface Nomination {
    nomination_id: string;
    nominee_id?: string;
    first_name?: string;
    date_submitted: string;
    closing_date: string;
    reason: string;
    process: string;
}